# ==========================================
# GRA-Genius-Swarm Dependencies
# Версия: 1.0 (Multiverse Obnulenko Core)
# ==========================================

# --- Ядро многоагентной архитектуры (LangGraph/LangChain) ---
# Необходимо для реализации цикла "Гений" и графов состояний
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- Высокопроизводительные вычисления (JAX) ---
# Критически важно для вычисления функционала пены Φ, градиентов и beauty_score
# Используется в math_core/foam_functional.py и math_core/beauty_score.py
jax>=0.4.30
jaxlib>=0.4.30
# Для работы с тензорами и линейной алгеброй в JAX
numpy>=1.26.0
scipy>=1.13.0

# --- Машинное обучение и нейросети ---
# Для эмбеддингов и интеграции с внешними моделями (DeepSeek wrapper)
torch>=2.2.0
transformers>=4.38.0
huggingface-hub>=0.21.0

# --- Математические бенчмарки и символические вычисления ---
# Необходимо для решения задач IMO, гипотезы Римана и проверки доказательств
sympy>=1.12
sagecell>=0.1.0  # Опционально: для продвинутой алгебры

# --- Веб-интерфейс и визуализация ---
# Для deploy/streamlit_app.py ("Рой vs Эйнштейн")
streamlit>=1.32.0
plotly>=5.19.0  # Для визуализации сходимости пены Φ и метрик роя

# --- Асинхронность и сеть (P2P, Telegram) ---
# Для swarm_engine/p2p_connector.py и связи агентов
aiohttp>=3.9.0
websockets>=12.0
python-telegram-bot>=21.0
fastapi>=0.110.0  # Если потребуется свой API сервер для роя
uvicorn>=0.27.0

# --- Утилиты и логирование ---
pydantic>=2.6.0  # Валидация данных состояний
tqdm>=4.66.0     # Прогресс-бары для симуляции мультиверса
rich>=13.7.0     # Красивые логи в терминале
pytest>=8.0.0    # Тестирование компонентов обнуления

# --- Облако и оркестрация (опционально, для масштабирования) ---
# kubernetes  # Раскомментировать при деплое на K8s/Yandex Cloud
# boto3       # Для AWS совместимых хранилищ состояний